- name: Bootstrap Python on clients
  hosts: clients
  gather_facts: false
  tasks:
    - name: Install Python on Ubuntu containers
      raw: |
        apt-get update
        apt-get install -y python3

- name: Deploy node_exporter on clients
  hosts: clients
  gather_facts: true
  roles:
    - node_exporter

